import type { Translations } from '@config/config.types';

export const translations: Translations = {
  search: {
    searchPlaceholder: 'Find hero...',
    searchButtonAriaLabel: 'Click to search',
  },
  heroCard: {
    checkBio: 'Check bio',
  },
};
